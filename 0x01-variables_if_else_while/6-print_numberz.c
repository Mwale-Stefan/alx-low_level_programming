#include <stdio.h>
/**
 * main - using the main function
 *
 * Description: print all singele digit numbers
 * prints all single digit numbers
 * Return: 0
 */
int main(void)
{
       	int i;

	for (i = 0 ; i <= 9 ; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
