#include <stdio.h>
/**
 * main - using the main function
 *
 * Description: all single digit numbers of base 10
 * prints all single digit numbers of base 10 and lne
 * Return: 0
 */
int main(void)
{
	int i;

	for (i = 0 ; i <= 9 ; i++)
	{
		printf("%i", i);
	}
	printf("\n");
	return (0);
}
